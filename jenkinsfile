pipeline {
    agent { 
                label 'agent-01'
            }
    stages {
        stage('Install Playwright') {
            steps {
                bat '''
                npm install playwright typescript --save-dev
                npm install tesseract.js
                '''
            }
        }
        stage('Run tests') {
            steps {
                bat '''
                npx playwright test --list
                npx playwright test tesseractdemo.test.ts --project chromium 
                '''
            }
            }
        }
    }
